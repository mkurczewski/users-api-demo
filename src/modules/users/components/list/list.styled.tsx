import styled from "styled-components"

export const ListWrapper = styled.ol`
  display: grid;
  grid-auto-rows: 3rem;
  counter-reset: user;
  align-self: start;
  justify-self: start;
  padding: 0;
`
